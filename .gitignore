# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.hypothesis/
*.whl

# Virtualenv
.venv/
venv/
env/
ENV/

# Node
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.yarn/

# Next.js
.next/
out/
.vercel/

# Turbo
.turbo/

# Build artifacts
dist/
build/
*.tsbuildinfo
*.vsix

# Editor & OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# Env & secrets
.env
.env.local
.env.*.local
*.key
*.pem

# Husk local artifacts (runtime)
.husk/
husk.db
husk.db-journal
husk.db-wal
husk.db-shm
# Stray SQLite from a benchmark/agent run rooted in the repo (the committed
# fixture is JSONL, so these patterns never touch it).
*.sqlite
*.sqlite-shm
*.sqlite-wal
/benchmark_research.sqlite
/traces.db
/traces.db-shm
/traces.db-wal

# Logs
logs/
*.log

# Test outputs
coverage/
playwright-report/
test-results/

# Misc
*.bak
*.tmp

# Sibling project tracked in its own repo
my-video/

# Stray source images accidentally dropped at the repo root
/IMG_*.jpeg
/IMG_*.png
/husk-before-after-*.png
/husk-hero-features*.png
/husk-video-ref-*.png

# Manus exports dropped at the repo root
/*.zip
/husk-ai-ui*/

# Claude Code working state
.claude/

# Internal docs not meant for the public repo
/*.pdf

# Enterprise edition (team / multi-tenant) — extracted from the public product,
# kept locally for a future private edition, never committed to the public repo.
/enterprise/

# Marketing material (overview copy, the Remotion ad project). Local-only: the ad
# carries its own node_modules with a bundled headless-Chrome binary and rendered
# .mp4 output, none of which belongs in the product repo.
/marketing/

# Vendored Remotion/mediabunny agent skills pulled for the marketing video above
# (101 files, ~half of them byte-duplicates). Nothing here relates to Husk itself.
/.agents/
/skills-lock.json

