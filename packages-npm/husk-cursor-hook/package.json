{
  "name": "husk-cursor-hook",
  "version": "0.1.0",
  "description": "Cursor observability bridge for Husk — streams file edits and stop signals into the local Studio.",
  "license": "BUSL-1.1",
  "bin": {
    "husk-cursor-hook": "./dist/cli.js"
  },
  "files": [
    "dist",
    "src/templates",
    "README.md"
  ],
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "node ./scripts/build.mjs --watch",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "esbuild": "^0.25.0",
    "typescript": "5.6.3"
  }
}
